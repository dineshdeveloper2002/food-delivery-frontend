name: workflow-trigger
on:
  push:
jobs:
 buildcheck:
  runs-on: ubuntu-latest
  steps:
    - name: Build started
      run: 'echo Execution started'
      
    - name: checkout
      uses: actions/checkout@v3
 webpackcompilation:
   needs: buildcheck
   runs-on: ubuntu-latest
   steps:
     - name: Build compilation
       run: 'echo npm run build ${{runner}}'
   
    
    
      
    
